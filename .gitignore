# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules/

# Build
dist/
dist-ssr/

# Local
*.local

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/

# Environment
.env
.env.local
.env.*.local

# Backups (모든 백업 제외)
*.backup
*.backup_*

# Test / Temp scripts (root)
__*.mjs

# Temp txt files in src/pages
src/pages/picker_*.txt
src/pages/timeline_*.txt

# Local DB
db/

# 분석 / 메모 .md (사장님 로컬 메모)
올잇_현재_상황_v*.md

# 일회성 마이그레이션 측 CSV (사장님 운영 데이터)
/신규_43주문_INSERT용.csv
.env.migrate
/유솔홈케어_운영.xlsx

# 이관 백업 / 운영 시트 / 옛 파일 backup (개인정보 포함)
scripts/backup-*.json
scripts/*.xlsx
*.before-*
# 운영 백업 디렉토리 (운영 데이터 + 비번 해시 포함 — commit 절대 금지)
backups/

# Google Calendar OAuth
gcal_credentials*.json
gcal_token.json
data/gcal-index.json

.vercel
