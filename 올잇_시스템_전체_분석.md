# 올잇 PWA — 시스템 전체 분석

**작성일**: 2026-05-03
**작성 기준**: V12-3 정정 적용 직후 (커밋 e3622bd 이후 / 미커밋 변경 포함)
**목적**: 디자이너/개발자가 시스템 전체를 이해하고 협업 대화 가능한 단일 자료
**원칙**: 코드에서 직접 확인한 사실만. 모르는 부분은 "확인 필요"로 표시.

> 이 문서는 코드베이스 직접 분석 결과. docs/ 폴더의 17개 .md 문서와는 별개. docs/와 코드가 일치하지 않는 부분은 "(docs vs 코드 불일치)" 표시.

---

## 0. 한 문단 요약

올잇은 에어컨 청소·냉매 충전 운영 플랫폼. **Vite + React 19 + Supabase + Google Apps Script** 스택. 현재 5개 페이지(LoginScreen / AdminApp / HappycallApp / EngineerApp / PrincipalApp) PWA. 데이터는 **localStorage 기반 mock + INITIAL_TASKS 시드**로 작동. Supabase 클라이언트는 `src/lib/supabase.js`에 박혀 있지만 PWA에서 호출하는 흔적 없음. 외부 백엔드는 `src/api.js`의 Google Apps Script URL 1개로 호출 가능하지만 PWA UI에서 사용하는 흔적 없음. 코드 내부 두 user 시스템(`shared/users.js` 4역할 + `data/users.js` 5역할)이 공존하며 App.jsx는 4역할 라우팅 사용.

---

## 1. 프로젝트 구조

### 1-1. 빌드 / 의존성 (`package.json`)

```json
{
  "name": "ollit",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vite build", "lint": "eslint .", "preview": "vite preview" }
}
```

**dependencies**:
- `@supabase/supabase-js` ^2.105.1
- `lucide-react` ^1.11.0
- `papaparse` ^5.5.3 (CSV 파싱 — 유솔 N CSV)
- `react` ^19.2.5
- `react-dom` ^19.2.5
- `xlsx` ^0.18.5 (엑셀 처리 — 유솔 N CSV/Excel)

**devDependencies**: vite ^8.0.10, @vitejs/plugin-react ^6.0.1, eslint ^10.2.1

**vite.config.js**: `defineConfig({ plugins: [react()] })` — 단순 React 플러그인만.

### 1-2. 폴더 구조

```
ollit/
├── package.json
├── vite.config.js
├── eslint.config.js
├── public/
│   ├── favicon.svg
│   ├── manifest.json (PWA manifest — 확인 필요)
├── src/
│   ├── App.jsx                  (62줄) — 메인 진입점, role 라우팅
│   ├── api.js                   (93줄) — Google Apps Script 호출 헬퍼
│   ├── index.css                (49줄) — 전역 + accent CSS 변수
│   ├── main.jsx                 (확인 필요 — 루트 마운트)
│   ├── lib/
│   │   └── supabase.js          (23줄) — Supabase client (PWA 용 anon)
│   ├── styles/
│   │   └── themes.js            (103줄) — DARK_THEME / LIGHT_THEME CSS 변수
│   ├── shared/
│   │   ├── TasksContext.jsx     (54줄) — useTasks() Provider
│   │   ├── tasks.js             (346줄) — INITIAL_TASKS 시드 (12건)
│   │   ├── users.js             (94줄) — REGISTERED_USERS (5건 mock)
│   │   ├── themes.js            (확인 필요 — 별도 themes 존재 / 폐기 가능)
│   │   └── taskAdapter.js       (확인 필요 — 시뮬 시트 호환용)
│   ├── data/                    (mock 데이터 + localStorage 헬퍼)
│   │   ├── tasks.js             (158줄) — ollit_tasks_v1 storage + makeEmptyTask
│   │   ├── seedTasks.js         (207줄) — USOL_N_SEED_TASKS 5건
│   │   ├── engineers.js         (266줄) — SEED_ENGINEERS 21명
│   │   ├── users.js             (108줄) — SEED_USERS 7건 + 5 ROLES + PERMISSIONS
│   │   ├── principals.js        (274줄) — SEED_PRINCIPALS 6 원청 + 정책 type
│   │   ├── regions.js           (175줄) — 33 지역 + 6 그룹
│   │   ├── taskStatus.js        (59줄) — 8 status enum + 액션 매트릭스
│   │   ├── serviceTypes.js      (64줄) — 4 작업 종류 (cleaning/refrigerant/visit/extra)
│   │   ├── standardRates.js     (70줄) — 단가표 + 쿨가이 가짜 단가
│   │   ├── visitFee.js          (32줄) — 출장비 30,000원 고정
│   │   ├── memos.js             (64줄) — ollit_task_memos_v1 (taskId 별)
│   │   ├── taskHistory.js       (118줄) — ollit_task_history_v1 (수정 이력)
│   │   ├── permissions.js       (190줄) — FIELD_PERMISSIONS / FIELD_EDIT_PERMISSIONS / 마스킹
│   │   ├── menuStructure.js     (50줄) — 일반 정산 메뉴 + 유솔 N 5탭
│   │   ├── dailyClose.js        (99줄) — 매일 마감 5원청 헬퍼
│   │   ├── usolNClassify.js     (68줄) — 유솔 N basic/extra 분류
│   │   ├── usolNOrderCsv.js     (80줄) — 네이버 발주서 CSV 파싱
│   │   └── usolNSettlementCsv.js(48줄) — 유솔 정산 CSV 파싱
│   ├── utils/
│   │   ├── commissionCalc.js    (121줄) — calcCleaning / calcRefrigerant
│   │   ├── usolNCommission.js   (78줄) — 유솔 N 85/15 수수료
│   │   ├── usolNAutoMatch.js    (46줄) — productOrderId 자동 매칭
│   │   ├── marginCalculator.js  (111줄) — 작업별 마진 분석
│   │   ├── dashboardStats.js    (148줄) — 메인 통계 / 역할별
│   │   ├── dailyAlertScheduler.js (163줄) — 매일 22시 자동 알림 (브라우저 setTimeout)
│   │   ├── notificationQueue.js (68줄) — ollit_notifications_v1 큐
│   │   └── engineerRecommendation.js (190줄) — 추천 점수 (지역 40 + 기종 20 + 일정 20 + 활동 10 + 등급 10)
│   ├── pages/
│   │   ├── LoginScreen.jsx      (892줄) — 위 분기 화면 (App.jsx 사용 X)
│   │   ├── AdminApp.jsx         (5942줄) — 운영자 (이대표 / admin role)
│   │   ├── HappycallApp.jsx     (2193줄) — 해피콜
│   │   ├── EngineerApp.jsx      (3237줄) — 기사
│   │   ├── PrincipalApp.jsx     (1416줄) — 원청
│   │   ├── picker_new.txt / timeline_v2~v4.txt (확인 필요 — 텍스트 백업)
│   ├── components/
│   │   ├── LoginScreen.jsx      (154줄) — 실제 사용되는 로그인 (App.jsx 진입)
│   │   ├── SplashScreen.jsx     (30줄)
│   │   ├── OllitMark.jsx (35줄) / OllitLoader.jsx (23줄)
│   │   ├── Chip.jsx             (45줄) — V12-3 신규 / 표준 칩
│   │   ├── EngineerBadge.jsx (106줄) / ServiceTypeBadge.jsx (42줄)
│   │   ├── AdminTaskDetailScreen.jsx (602줄) — V12-3 신규 / 운영자 작업상세 재설계
│   │   ├── SettlementScreen.jsx (629줄) — 정산 (5원청 매일 마감)
│   │   ├── PrincipalSettlementScreen.jsx (377줄) — 회사→원청 정산
│   │   ├── EngineerSettlementScreen.jsx (477줄) — 기사 본인 정산
│   │   ├── EngineerListScreen.jsx (189줄) / EngineerEditScreen.jsx (340줄)
│   │   ├── EngineerRecommendCard.jsx (107줄) / EngineerRegionEditor.jsx (197줄)
│   │   ├── PrincipalListScreen.jsx (171줄) / PrincipalEditScreen.jsx (659줄)
│   │   ├── PrincipalDetailScreen.jsx (315줄)
│   │   ├── RegionListScreen.jsx (800줄) / RegionEditScreen.jsx (188줄) / RegionEngineerSearch.jsx (286줄)
│   │   ├── UserListScreen.jsx (137줄) / UserEditScreen.jsx (332줄)
│   │   ├── RatesManagementScreen.jsx (232줄) / NaverUploadScreen.jsx (419줄)
│   │   ├── TaskCardMenu.jsx (156줄) / TaskCompleteScreen.jsx (226줄) / TaskEditScreen.jsx (489줄) / TaskHistoryScreen.jsx (155줄)
│   │   ├── MemoAddScreen.jsx (145줄) / VisitOnlyDialog.jsx (151줄)
│   │   ├── AllEngineersModal.jsx (211줄) / NotificationsScreen.jsx (193줄)
│   │   ├── SettingsScreen.jsx (280줄) / UsolNScreen.jsx (300줄)
│   │   └── usol_n/
│   │       ├── UsolNOrders.jsx / UsolNInProgress.jsx / UsolNCsvMatch.jsx / UsolNTracking.jsx / UsolNEngineerSettlement.jsx
├── docs/                        (17개 .md 설계 문서 — 5,684줄)
└── db/migrations/
    ├── 001_init.sql             (348줄) — 23 테이블 + 6 인덱스 + pgcrypto
    ├── 001_init.sql3            (확인 필요 — 무엇인지)
    ├── 002_drop_all.sql         — public schema 전체 DROP
    └── README.md
```

### 1-3. 메인 진입점 / 라우팅 (`src/App.jsx`)

```jsx
function App() {
  const [splashDone, setSplashDone] = useState(false);
  const [currentUser, setCurrentUser] = useState(null);

  useEffect(() => { applyTheme(loadTheme()); }, []);

  if (!splashDone) return <SplashScreen onDone={...}/>;

  switch (currentUser.role) {
    case "engineer":  return <EngineerApp .../>;
    case "happycall": return <HappycallApp .../>;
    case "admin":     return <AdminApp .../>;
    case "principal": return <PrincipalApp .../>;
  }
  return <LoginScreen onLogin={handleLogin}/>;  // 비로그인
}
```

**중요**: App.jsx는 **4역할(engineer/happycall/admin/principal)**만 분기. data/users.js의 5역할(owner/admin/happycall/engineer/principal)과 다름. App.jsx는 `shared/users.js`의 `REGISTERED_USERS`를 사용하는 `components/LoginScreen.jsx`를 통해 인증.

전체 앱은 `<TasksProvider>` 안에 있어 모든 페이지가 `useTasks()` 통해 같은 task state 공유 (`shared/TasksContext.jsx`).

### 1-4. 백엔드 통신 — `src/api.js`

`API_URL = "https://script.google.com/macros/s/AKfycbxow-YEIiKCIf5nuEG1s0qb2N3JgXrpzDZnV03Dt57yIvXtC05jpq3XF2HVBjemI1Gl/exec"`

노출된 함수: `ping()`, `getSystemInfo()`, `login(userId, password)`, `getTasks(role, userId, principalCode)`, `getTaskDetail(taskId)`, `createTask(taskData)`, `updateTaskStatus(taskId, status, updates)`, `parseKakao(text)`, `getOffDays(engineer)`, `getOffDaysByDate(date)`, `addOffDay(...)`, `deleteOffDay(offId)`.

**현재 사용 흔적 (PWA 화면에서 import)**:
- `src/pages/HappycallApp.jsx:2` → `import { updateTaskStatus, getTasks } from "../api.js"`
- 그 외: AdminApp / EngineerApp / PrincipalApp 모두 api.js를 import 안 함
- 결론: **api.js는 거의 사용 안 됨**. HappycallApp만 일부 호출 가능 (실제 호출 빈도는 확인 필요)

### 1-5. Supabase — `src/lib/supabase.js`

```jsx
const url = import.meta.env.VITE_SUPABASE_URL;
const anonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;
if (!url || !anonKey) throw new Error("Supabase 환경변수 누락");

export const supabase = createClient(url, anonKey, {
  auth: {
    persistSession: true, autoRefreshToken: true, detectSessionInUrl: false,
    storage: window.localStorage, storageKey: "allit.auth",
  },
});
```

**현재 사용 흔적**: src 전체에서 `import.*supabase\.js` grep 결과 = **0건**. `.env.local` 환경변수 미설정 시 throw 발생 → 앱이 죽는 위험. 현재는 import 자체를 안 하므로 죽지 않음.

→ **Supabase는 코드 베이스에만 박혀 있고 실제로 작동 X**. 모든 데이터는 localStorage + INITIAL_TASKS.

---

## 2. 데이터 모델 (전체)

### 2-1. 작업 (Task) — 두 시스템 공존

#### 2-1-A. shared/tasks.js (INITIAL_TASKS — 12건 시드)

`shared/tasks.js:10` `INITIAL_TASKS` 배열의 한 행 필드 (실측):

```js
{
  id: "A260427-001",
  client: "올데이케어",                                    // 원청 이름 (한글)
  customer: "박지영", phone: "010-2345-6789",
  address: "강남구 역삼동",                                 // 요약
  fullAddress: "테헤란로 152, 강남파이낸스센터 25층",       // 상세
  workType: "세척",                                        // 세척 / 세척+점검 / 냉매충전 / 점검 / 설치 / 이전설치 / 수리
  appliance: "벽걸이",                                     // 벽걸이 / 스탠드 / 시스템 멀티 / 4way / ...
  qty: 1,
  requestedDate: "2026-04-27", requestedTime: "오전",       // 오전/낮 시간/오후/저녁
  receivedAt: "2026.04.25", receivedAgo: "10분 전",         // 시간 표시 (문자열)
  channel: "카카오톡",                                     // 카카오톡 / 전화 / 직접 / 네이버 / 지인소개
  happycallStatus: "assigned",                             // uncontacted / contacted / assigned
  happycallMemo: "고객님 친절하셨음. 시간 약속 잘 지키세요.",
  requestNote: "현관 비밀번호 1234, 강아지 있어요",
  assignedEngineer: "김동효", assignedEngineerId: "E001",
  recommendedEngineer: null,
  scheduledDate: "2026-04-27", scheduledTime: "09:00",
  time: "09:00", endTime: "10:30", duration: "1.5h",       // EngineerApp 호환
  distance: "12.4km", travelTime: "32분",
  status: "진행중",                                         // 한글 — 약속대기 / 확정 / 진행중 / 완료
  startedAt: "09:05", completedAt: null,
  estimateTotal: 80000, productPrice: 80000, travelFee: 0,
  extraFee: 0, extraReason: "",
  commissionRate: 40, commission: 32000, engineerNet: 48000,
  workMemo: "", beforePhoto: false, afterPhoto: false,
  scheduleHistory: [],
  isUrgent: false, urgentReason: "당일 작업 요청",          // isUrgent=true 시
}
```

#### 2-1-B. data/tasks.js (`makeEmptyTask` — V11-1 신규 형식)

`data/tasks.js:97` `makeEmptyTask({})` 기본값:

```js
{
  id: generateTaskId(),
  // 식별
  taskCode: "", productOrderId: null, orderNumber: null,
  orderType: null,    // 'basic' | 'extra' (유솔 N)
  productName: null, optionInfo: null,
  // 작업
  workType: "", appliance: "", qty: 1,
  workItems: null,    // [{ type: '4way', count: 1 }, ...]
  // 고객
  customer: "", phone: "", address: "", region: "", request: "",
  buyerName: null, buyerPhone: null,
  // 일정
  schedule: "", time: "", workDate: null,
  // 금액 (일반)
  estimateTotal: 0, addonFee: 0, engineerEarning: 0,
  companyMargin: 0, principalFee: 0,
  // 금액 (유솔 N)
  grossAmount: 0, npayFee: 0, salesLinkFee: 0, netAmount: 0,
  // 시점
  receivedAt: null, completedAt: null, engineerPaidAt: null,
  // 시점 — 유솔 N (3시점)
  paidAt: null,            // 네이버 결제일
  naverSettledAt: null,    // 네이버 정산완료일
  companyReceivedAt: null, // 회사 받은 날 (월요일)
  engineerSettledAt: null, // 기사에게 입금한 날 (15일)
  settlementStatus: null, settlementBaseDate: null,
  // 배정
  principal: "", principalId: "",
  engineer: "", engineerId: "",
  state: "waiting",        // waiting / scheduled / moving / active / done — 영문
  status: "received",      // 영문 enum (taskStatus.js 참조)
  // CSV
  importCsvType: null, csvImportedAt: null,
}
```

**중요 — 두 시스템의 status 차이**:
- `shared/tasks.js` task.status: 한글 (`"약속대기" / "확정" / "진행중" / "완료"`)
- `data/tasks.js` task.status: 영문 (`"received" / "assigned" / "confirmed" / "in_progress" / "completed" / "partial" / "visit_only" / "canceled"`)
- AdminApp.jsx는 `state` 필드 사용 (`"waiting" / "scheduled" / "moving" / "active" / "done"`)

`TaskCardMenu.jsx:8` `deriveStatus()`가 state → status 매핑 처리:
- `active`/`moving` → `in_progress`
- `waiting` → `received`
- `scheduled` → `confirmed`
- `done` → `completed`

#### 2-1-C. SQL 스키마 (db/migrations/001_init.sql `tasks` 테이블)

```sql
CREATE TABLE tasks (
  id, tenant_id, category_id, task_no, principal_id,
  customer_name, phone, address, district,
  channel, request_note, is_urgent,
  status text DEFAULT '미배정' CHECK (status IN ('미배정','약속대기','확정','진행중','완료','취소')),
  happycall_status, happycall_memo, happycall_internal_note,
  recommended_engineer_id, assigned_engineer_id,
  assignment_type text DEFAULT 'manual' CHECK (... 'manual','auto_first_accept','manual_with_recommendation'),
  requested_date, requested_time, scheduled_at,
  started_at, completed_at, work_memo,
  product_price, travel_fee, extra_fee, extra_reason,
  total_amount GENERATED ALWAYS AS (product+extra+travel) STORED,
  category_data jsonb DEFAULT '{}',
  received_at, created_at, updated_at,
  UNIQUE (tenant_id, task_no)
);
```

→ DB는 한글 enum 6단계 (미배정/약속대기/확정/진행중/완료/취소). PWA 코드와 다름. **확인 필요** (Phase 1B 마이그레이션 시 정리 필요).

### 2-2. 기사 (Engineer) — `src/data/engineers.js`

storage key: `ollit_engineers_v1` / 시드 21명 (V11-5 시드 / 명단은 docs `_NEXT_SESSION.md` 28명과 다름).

```js
{
  id: "yang_seungmoon",          // string id (snake_case 영문)
  name: "양승문",
  phone: "010-3749-0294",
  careerLevel: "expert",          // rookie / career / expert
  status: "active",                // active / off / quit
  workTypes: {
    cleaning:    { role: "main", zones: ["고양시","은평구","서대문구"], appliances: [] },
    refrigerant: { role: "main", zones: ["은평구"], appliances: [] },
  },
  // role: "main" / "backup" / "sub" / "none"
  // appliances: [] (빈 배열 = 모두) 또는 ["벽걸이"] 등
  note: "",
}
```

**enum**:
- `CAREER_LEVELS`: rookie (신입 #888780) / career (경력 #00875A) / expert (전문가 #D45A8C)
- `STATUS_OPTIONS`: active / off / quit
- `ROLE_OPTIONS`: main (메인) / backup (백업) / none (안 함)
- `APPLIANCE_OPTIONS`: ["벽걸이", "스탠드", "천장형", "원웨이"]
- `SEOUL_DISTRICTS`: 25구 / `GG_INCHEON`: 8개 시군

시드 21명 분류:
- **세척 메인 10**: 양승문 / 김윤섭 / 정상현 / 안승웅 / 김영수 / 김현동 / 임종일 / 류근학 / 정훈 / 전현진
- **벽걸이 전문가 2**: 권창용 / 이상준
- **세척 백업 + 냉매 메인 6**: 김재현 / 김태승 / 문성목 / 손동식 / 김병철 / 김동효
- **냉매 전용 2**: 변기현 / 강병익

(docs/_NEXT_SESSION.md 28명 명단과 다름 — Phase 1A 시드는 21명만 적용됨)

### 2-3. 원청 (Principal) — `src/data/principals.js`

storage key: `ollit_principals_v1` / 시드 6개

```js
{
  id: "allday",
  name: "올데이케어", nickname: "자체영업",
  color: "#FF1B8D", prefix: "A-",
  status: "active",                  // active / off
  vatPolicy: "included",             // included (그대로) / excluded (×1.10)
  contact: { manager: "", phone: "", email: "" },
  note: "직영 / 원청 수수료 0",
  commissionPolicy: {
    cleaning: { type, principal, fakeRates?, splitRate?, additionalsPolicy?, engineer? },
    refrigerant: { type, principal, engineer },
  },
}
```

**정책 type 3종**:
- `standard` — 표준 (원청 수수료 + 기사 단가표 적용)
- `fake_split` — 쿨가이형 ((총금액 - 가짜 단가) × split%)
- `naver_settlement` — 네이버형 (정산예정금액 기준 + 추가선택 별도 분배)

**6 원청 정책 (시드 그대로)**:

| id | 회사 | 색 | prefix | vat | 세척 정책 | 냉매 정책 |
|---|---|---|---|---|---|---|
| `allday` | 올데이케어 | #FF1B8D | A- | included | standard / 원청 0 | standard / 원청 0 / 기사 50% |
| `aircon_pro` | 에어컨프로 (쿨가이) | #06B6D4 | A- | included | fake_split / split 50% | standard / 원청 10% / 기사 50% |
| `yongin` | 용인컴퍼니 | #888780 | A- | included | standard / 원청 정액 10,000 | standard / 원청 10,000 / 기사 50% |
| `usol_h` | 유솔홈케어 H | #10B981 | YS- | included | standard / 원청 15% | standard / 원청 0 / 기사 50% |
| `usol_n` | 유솔홈케어 N | #03C75A | YS-N- | excluded | naver_settlement / 정산예정×15% / 추가선택 (15/85) | null (없음) |
| `crikrin` | 크리크린 | #7F77DD | CK- | included | standard / 원청 20% | standard / 원청 20% (견적 기준) / 기사 50% |

**docs vs 코드 불일치**: docs는 KA(쿨가이 아버지) / KB(쿨가이 아들) 분리를 박았는데, 코드 시드는 단일 `aircon_pro`만 존재.

`fakeRates` (쿨가이 가짜 단가 — 기사 노출용):
```js
{ "벽걸이": 50000, "스탠드": 70000, "투인원": 110000, "1way": 60000, "4way": 80000, "원형": 90000 }
```

`SEED_RATES.cleaning` (실제 기사 단가):
```js
{ "벽걸이": 40000, "스탠드": 60000, "투인원": 100000, "1way": 50000, "4way": 70000, "원형": 80000 }
```

→ 차이 = 사장님 마진 (정보 비대칭 패턴)

**상수**:
- `COMMISSION_TYPES`: none / rate (%) / fixed (원)
- `COMMISSION_BASE`: estimate (견적금액) / total (총금액) / settlement (정산예정금액)
- `STATUS_OPTIONS`: active / off
- `VAT_OPTIONS`: included / excluded (기사 단가 ×1.10)
- `PRINCIPAL_COLORS` (10색): #FF1B8D / #06B6D4 / #A855F7 / #10B981 / #F59E0B / #7F77DD / #00875A / #FF3D5A / #EC4899 / #3B82F6

### 2-4. 사용자 (User) — 두 시스템

#### 2-4-A. shared/users.js (실제 로그인용 — `REGISTERED_USERS`)

5명 mock:

| userId | name | role | engineerId/principalId | password |
|---|---|---|---|---|
| `kim.donghyo` | 김동효 | engineer | E001 | engineer1! |
| `lee.jaehyun` | 이재현 | engineer | E002 (firstLogin) | temp1234 |
| `kim.jihye` | 김지혜 | happycall | — | happycall1! |
| `lee.ceo` | 이대표 | admin | — | admin1234! |
| `kim.coolguy` | 김쿨가이 | principal | clientName=쿨가이 | cool1234! |

`ROLE_INFO` 매핑: engineer (🔧 #10B981) / happycall (📞 #7F77DD) / admin (👔 #E91860) / principal (🏪 #FFB800)

#### 2-4-B. data/users.js (5역할 권한 시스템 — `SEED_USERS` 7명)

```js
ROLES = {
  owner:     { name: "대표 / 운영자", color: "#FF1B8D", desc: "전체 권한" },
  admin:     { name: "관리자",        color: "#1A1512", desc: "배정 / 정산" },
  happycall: { name: "해피콜",        color: "#888780", desc: "접수 / 일정 조율" },
  engineer:  { name: "기사",          color: "#00875A", desc: "자기 작업만" },
  principal: { name: "원청",          color: "#FF1B8D", desc: "자기 원청 작업만" },
}
```

`getCurrentUser()` 매핑 — `appUser.role === "admin" && userId === "lee.ceo"` 일 때만 owner로 승격. 그 외 admin은 그대로.

`PERMISSIONS` (메뉴 권한 매트릭스):

| 권한 키 | owner | admin | happycall | engineer | principal |
|---|---|---|---|---|---|
| menu.dashboard | ✓ | ✓ | ✓ |  |  |
| menu.new_order | ✓ | ✓ | ✓ |  |  |
| menu.in_progress | ✓ | ✓ | ✓ | ✓ | ✓ |
| menu.settlement | ✓ | ✓ |  |  |  |
| menu.engineers | ✓ | ✓ |  |  |  |
| menu.principals | ✓ | ✓ |  |  |  |
| menu.rates | ✓ |  |  |  |  |
| menu.regions | ✓ | ✓ |  |  |  |
| menu.users | ✓ |  |  |  |  |
| menu.notifications | ✓ | ✓ |  |  |  |
| menu.backup | ✓ |  |  |  |  |
| task.assign | ✓ | ✓ |  |  |  |
| task.cancel | ✓ | ✓ | ✓ |  |  |
| task.complete | ✓ | ✓ |  | ✓ |  |
| task.edit_price | ✓ | ✓ |  |  |  |

`MENU_PERMISSIONS` (유솔 N 별도):
- `menu:usol_n` → owner / admin
- `menu:usol_n_orders` / `menu:usol_n_in_progress` → owner / admin / happycall
- `menu:usol_n_csv_match` / `menu:usol_n_tracking` / `menu:usol_n_engineer_settlement` → owner / admin

### 2-5. 필드 권한 — `data/permissions.js`

`FIELD_PERMISSIONS` (보기):

| 필드 | 허용 |
|---|---|
| task.customer_phone_full | owner / admin / happycall / engineer |
| task.customer_phone_mask | principal |
| task.total_amount | 모두 |
| task.engineer_rate | owner / admin / engineer (자기) |
| task.principal_fee | owner / admin / principal (자기) |
| task.company_margin | owner / admin |
| task.fake_engineer_rate | owner |
| task.other_engineers | owner / admin / happycall |
| task.other_principals | owner / admin / happycall |
| task.upload_naver | owner / admin |
| task.netAmount | owner / admin / happycall / engineer |
| task.companyReceive | owner / admin |
| task.companyMargin | owner |
| task.naverSettledAt | owner / admin / engineer |
| task.companyReceivedAt | owner / admin |
| task.engineerSettledAt | owner / admin / engineer |

`FIELD_EDIT_PERMISSIONS` (편집): 위와 별도. 금액(`engineerEarning` / `companyMargin`) = owner only. `principalId` / `principal` = owner only. 그 외 대부분은 owner / admin / happycall.

`maskPhone(phone)`: `010-1234-5678` → `010-****-5678` (principal 역할에 적용).

`filterTasksForUser(tasks, user)`:
- owner / admin / happycall → 전부
- engineer → `engineerId === user.engineerId || engineer === user.name`
- principal → `principalId === user.principalId` (또는 한글 이름 매칭 fallback)

### 2-6. 정산 (Settlement) 데이터

#### 2-6-A. 매일 마감 5원청 (`data/dailyClose.js`)

대상: `["allday", "aircon_pro", "yongin", "usol_h", "crikrin"]` (유솔 N 제외)
완료 상태: `["completed", "partial", "visit_only", "done"]`

`getDailyCloseStatus(date)`:
- `expected` = 합계 (회사가 받을 돈)
- `received` = `engineerPaidAt` 있는 합계
- `unpaid` = expected - received

`pickAmountFromEngineer(t)`:
- `t.companyReceiveFromEngineer` 우선
- 없으면 `(estimateTotal + addonFee) - engineerEarning`

`getUnpaidByEngineer()`:
- 미입금 작업을 기사별로 그룹
- 출력 필드: `engineerId / engineerName / tasks / totalAmount / unpaidDays / unpaidDates`

#### 2-6-B. 출장비 (`data/visitFee.js`)

```js
VISIT_FEE = { amount: 30000, engineerShare: 100, companyShare: 0, principalShare: 0 }
VISIT_REASONS = [
  { id: "wrong_type",      label: "기종이 의뢰와 다름" },
  { id: "no_access",       label: "접근 불가 (사다리차/높이)" },
  { id: "customer_absent", label: "고객 부재" },
  { id: "other",           label: "기타" },
]
```

#### 2-6-C. 유솔 N 수수료 (`utils/usolNCommission.js`)

```js
COMPANY_COMMISSION_RATE = 0.85   // 회사 = 85% / 유솔 = 15%

// 기본 작업 (basic) — 기사는 단가표 / 회사 = 받음 - 단가
// 추가선택 (extra) — 기사 100% / 회사 마진 0
calcCompanyReceive(task)  = floor(netAmount × 0.85)
calcEngineerEarning(task) = orderType === "extra" ? companyReceive : basicEngineerRate
calcCompanyMargin(task)   = companyReceive - engineerEarning
```

#### 2-6-D. payments SQL (db/migrations/001_init.sql)

```sql
status CHECK (status IN ('미정산','사장님입금','기사확인','정산완료','보류','분쟁','지연'))
paid_at         -- 사장님 [입금 완료] 클릭 시각
confirmed_at    -- 기사 [입금 확인] 시각 (원청 정산은 NULL)
settled_at      -- 정산완료 시각
expected_settlement_date / actual_settlement_date
is_balanced GENERATED ALWAYS AS ((engineer + principal + owner) = (product + extra + travel)) STORED
```

### 2-7. localStorage 키 전체

| 키 | 위치 | 형식 |
|---|---|---|
| `ollit_engineers_v1` | data/engineers.js | engineer[] |
| `ollit_principals_v1` | data/principals.js | principal[] |
| `ollit_regions_v1` | data/regions.js | region[] |
| `ollit_region_groups_active_v1` | data/regions.js | { groupId: bool } |
| `ollit_standard_rates_v1` | data/standardRates.js | { cleaning: {}, coolguyFake: {} } |
| `ollit_users_v1` | data/users.js | user[] |
| `ollit_tasks_v1` | data/tasks.js | task[] (CSV 임포트 / 유솔 N 시드) |
| `ollit_task_memos_v1` | data/memos.js | { [taskId]: memo[] } |
| `ollit_task_history_v1` | data/taskHistory.js | history[] |
| `ollit_notifications_v1` | utils/notificationQueue.js | notification[] (max 100) |
| `ollit_last_alert_date` | utils/dailyAlertScheduler.js | "YYYY-MM-DD" |
| `ollit_theme` | styles/themes.js | "light" / "dark" |
| `allit.auth` | lib/supabase.js (storageKey) | Supabase session (PWA가 supabase 미사용 → 비어 있음) |

### 2-8. 모든 enum 종합

#### 2-8-A. taskStatus.js (영문 8단계)

```js
TASK_STATUSES = {
  received:    { name: "접수",     emoji: "🟠", color: "#888780" },
  assigned:    { name: "배정",     emoji: "🟡", color: "#FF1B8D" },
  confirmed:   { name: "확정",     emoji: "🔵", color: "#06B6D4" },
  in_progress: { name: "진행중",   emoji: "🟢", color: "#00875A" },
  completed:   { name: "완료",     emoji: "⚪", color: "#00875A" },
  partial:     { name: "부분완료", emoji: "🔶", color: "#FF1B8D" },
  visit_only:  { name: "출장비만", emoji: "🚗", color: "#FF1B8D" },
  canceled:    { name: "취소",     emoji: "⚫", color: "#888780" },
}
ACTIVE_STATUSES = ["received","assigned","confirmed","in_progress"]
FINAL_STATUSES  = ["completed","partial","visit_only","canceled"]

STATUS_ACTIONS = {
  received:    ["assign","cancel"],
  assigned:    ["confirm","cancel"],
  confirmed:   ["start","cancel"],
  in_progress: ["complete","partial","visit_only","cancel"],
}

ACTION_LABELS = { assign:"기사 배정", confirm:"고객 확정 처리", start:"작업 시작",
  complete:"✓ 정상 완료", partial:"🔶 부분 완료", visit_only:"🚗 출장비만 정산", cancel:"⚫ 작업 취소" }

CANCEL_REASONS = [customer / schedule / onsite / other] (V12-3 AdminTaskDetailScreen에 동일 4사유)
```

#### 2-8-B. AdminApp 영문 state (`src/pages/AdminApp.jsx`)

`task.state`: `"waiting"` (대기) / `"scheduled"` (예정) / `"moving"` (이동중) / `"active"` (진행중) / `"done"` (완료)
`task.type`: `"work"` (작업) / `"external"` (외근)
`task.assignType`: `"assigned"` / `"confirmed"`

#### 2-8-C. SQL DB enum (db/migrations/001_init.sql — 한글 6단계)

```sql
tasks.status CHECK IN ('미배정','약속대기','확정','진행중','완료','취소')
payments.status CHECK IN ('미정산','사장님입금','기사확인','정산완료','보류','분쟁','지연')
tasks.assignment_type CHECK IN ('manual','auto_first_accept','manual_with_recommendation')
task_assignments.status CHECK IN ('대기','수락','거절','마감')
user_roles.role CHECK IN ('owner','operator','engineer','partner','admin')
users.phone_label CHECK IN ('main','gas','work')
engineer_permissions.level CHECK IN ('main','sub')
commission_policies.calc_method CHECK IN ('비율','차감후비율','금액','수량비율','AG전체_AD반반','예상금액비율','비율_총액기준','추가선택')
```

#### 2-8-D. 작업 종류 (`data/serviceTypes.js`) — V11-9 / V12-3

```js
SERVICE_TYPES = {
  cleaning:    { id, label:"세척", icon:"❄️", color:"#00BCD4", textColor, bgColor:"var(--bg-secondary)" /* V12-3 */, borderColor:"#00BCD4" },
  refrigerant: { id, label:"냉매", icon:"⚡", color:"#FFB300", ... },
  visit:       { id, label:"출장", icon:"🚗", color:"#555555", ... },
  extra:       { id, label:"추가", icon:"➕", color:"#FF8F00", ... },
}
```

`detectServiceType(task)` 우선순위:
1. `task.status === "visit_only"` → visit
2. `task.orderType === "extra"` → extra
3. `task.workType === "냉매충전"` → refrigerant
4. 텍스트에 "냉매"/"가스"/"충전" 포함 → refrigerant
5. 텍스트에 "출장" 포함 (단 "청소"/"세척" 제외) → visit
6. 그 외 → cleaning

#### 2-8-E. 메모 (`data/memos.js`)

```js
MEMO_TYPES = [
  { id:"general", emoji:"📝", label:"일반 메모" },
  { id:"call",    emoji:"📞", label:"통화 내용" },
  { id:"issue",   emoji:"⚠️", label:"이슈/특이사항" },
]
```

---

## 3. 사용자 역할 (5개)

### 3-1. 코드 상의 역할 정의 — 두 시스템 공존

**App.jsx (실제 로그인 분기)**: 4 역할
- `engineer` → EngineerApp
- `happycall` → HappycallApp
- `admin` → AdminApp
- `principal` → PrincipalApp
- 그 외 / null → LoginScreen

**data/users.js (권한 매트릭스)**: 5 역할
- `owner` (대표 / 운영자) — admin 중 lee.ceo만 매핑
- `admin` (관리자) — 그 외 admin
- `happycall` (해피콜)
- `engineer` (기사)
- `principal` (원청)

**SQL 스키마 user_roles.role**: `owner / operator / engineer / partner / admin` (5개, 다른 이름)

→ **확인 필요**: 코드 / SQL / docs 의 role 명칭이 모두 다름.

### 3-2. 역할별 화면 (App.jsx 기준)

#### 3-2-A. engineer (기사) → EngineerApp.jsx

**탭 4개** (BottomTabBar):
- `today` (오늘) — MainScreen + ActionAlert + CompactTaskCard 리스트 + CalendarScreen 진입
- `settle` (정산) — SettlementScreen (월별 / JobSettlementCard)
- `noti` (알림) — NotificationsScreen
- `profile` (프로필) — ProfileScreen + ProfileMenuItem

**내부 화면**:
- TaskDetailScreen (작업 상세 — 시작/완료 토글)
- CompletionReportScreen (완료 보고 — 사진 + extra_fee)
- CalendarScreen (월/주 캘린더)
- DayDetailScreen (날짜별 + 휴무 등록)
- TimeWheelSheet / DragTimePicker / AddOffDayModal

**필터**: `filterTasksForEngineer(allTasks, user.engineerId)` — 본인 작업만.

#### 3-2-B. happycall (해피콜) → HappycallApp.jsx

**screen state 분기**:
- `main` — HappycallMainScreen (ActionAlert + 탭 / TaskCard 리스트)
- `newReception` — NewReceptionScreen (카톡 paste / 직접 입력)
- `edit` — HappycallEditScreen (작업 수정)
- `assign` — AssignEngineerScreen (기사 배정 + EngineerTimelineCard)
- `memo` — MemoEditScreen (해피콜 메모)

**내장 mock 데이터**: `ENGINEERS` 5명 (E001~E005, 일정 풀 캘린더 포함). 자체 정의로 data/engineers.js와 별개.

#### 3-2-C. admin (운영자, 이대표) → AdminApp.jsx (5942줄 ★)

**대시보드 탭 4개** (`dashboardActiveTab`):
- `overview` (개요) — OverviewTab
- `tasks` (작업)
- `engineers` (기사) — EngineersTab + EngineerCard / EngineerActivityCard
- `activities` (활동) — 확인 필요

**screen 분기 (모든 화면 — 31개)**:

| screen | 컴포넌트 | 라인 |
|---|---|---|
| `null` (기본) | DashboardScreen | 1940 |
| newReception | NewReceptionScreen + ReceptionGroup + CleaningCard / RefrigerantCard | 2689 |
| newReceptionForm | NewReceptionFormScreen (해피콜 시각의 신규 등록 폼) | 5256 |
| memoAdd | (외부 컴포넌트) MemoAddScreen | — |
| taskEdit | (외부 컴포넌트) TaskEditScreen | — |
| taskHistory | (외부 컴포넌트) TaskHistoryScreen | — |
| notifications | NotificationScreen | 5110 |
| liveWork | LiveWorkScreen + LiveWorkContent + TaskGroupSection / ActivityGroupSection | 3221 |
| engineerDay | EngineerDayScreen + TaskCard | 3836 |
| taskDetail | **AdminTaskDetailScreen** (V12-3 신규) | (외부) |
| recommend | RecommendScreen + RecommendCard | 4876 |
| autoAssign | AutoAssignScreen | 4601 |
| assignedList | AssignedTasksScreen + AssignedCard | 2580 |
| inProgressList | InProgressListScreen | 3251 |
| settlement | SettlementDailyClose | (외부) |
| principal_settlement | PrincipalSettlementScreen | (외부) |
| engineerList | EngineerListScreen | (외부) |
| engineerEdit | EngineerEditScreen | (외부) |
| regionList | RegionListScreen | (외부) |
| regionEdit | RegionEditScreen | (외부) |
| settings | SettingsScreen | (외부) |
| usol_n | UsolNScreen (5탭 컨테이너) | (외부) |
| userList | UserListScreen | (외부) |
| userEdit | UserEditScreen | (외부) |
| notificationSettings | NotiSettingsScreen | (외부) |
| principalList | PrincipalListScreen | (외부) |
| principalEdit | PrincipalEditScreen | (외부) |
| ratesManagement | RatesManagementScreen | (외부) |
| naverUpload | NaverUploadScreen | (외부) |

**메인 통계 (TODAY_STATS)**: newReceived / assigned / confirmed / inProgress / completed / revenue / myMargin / engineerNet / principalFee

**보유 메뉴** (`SettingsScreen` 통해 접근): 정산, 회사→원청 정산, 기사 관리, 원청 관리, 단가 관리, 지역 관리, 사용자 관리, 알림 설정, 유솔 N (조건부)

#### 3-2-D. principal (원청, 김쿨가이) → PrincipalApp.jsx

**탭 4개** (BottomNav):
- `list` — ListTab + TaskCard 리스트
- `new` — NewTab + parseKakao 자동 입력 + Field 폼
- `settle` — SettleTab (자기 원청 수수료만)
- `info` — InfoTab

**내부 화면**:
- TaskDetail (자기 의뢰만 — partner-safe view)
- SubmittedScreen (등록 완료)

**필터**: `filterTasksForPrincipal(allTasks, user.clientName)` — 자기 원청 작업만.

### 3-3. 권한 매트릭스 (코드에서 박힌 그대로)

위 2-4-B와 2-5 참조.

**핵심 액션 권한**:
- `task.assign` (배정) — owner / admin
- `task.cancel` (취소) — owner / admin / happycall
- `task.complete` (완료) — owner / admin / engineer
- `task.edit_price` (금액 편집) — owner / admin

**핵심 보기 권한**:
- 회사 마진 (`task.company_margin`) — owner / admin only
- 가짜 단가 (`task.fake_engineer_rate`, 쿨가이 비밀) — owner only
- 다른 기사/원청 정보 (`task.other_engineers/principals`) — owner / admin / happycall

**핵심 편집 권한** (`FIELD_EDIT_PERMISSIONS`):
- `engineerEarning` / `companyMargin` — owner only
- `principalId` / `principal` — owner only
- 기타 작업 정보 — owner / admin / happycall (memo는 engineer 추가)

### 3-4. 메인 대시보드 차이

- **engineer (기사)**: 오늘 일정 + 작업 시작/완료 액션 (자기 것만)
- **happycall**: 새 접수 + 미배정 작업 + 기사 배정 화면 (정산 X)
- **admin (이대표)**: 4구역 통계 (작업/돈) + 4탭 (개요/작업/기사/활동) + 모든 메뉴 진입
- **principal (쿨가이)**: 자기 원청 작업 리스트 + 신규 등록 + 자기 정산 (회사 마진 X)

---

## 4. 작업 생명주기

### 4-1. status 단계 (영문 enum 기준)

```
received (접수)        ⟶ 새 접수 들어옴 (uncontacted/Pre 해피콜)
   ↓ assign
assigned (배정)        ⟶ 기사 배정됨, 일정 미확정
   ↓ confirm
confirmed (확정)       ⟶ 일정까지 확정 (고객+기사 합의)
   ↓ start
in_progress (진행중)   ⟶ 작업 시작
   ↓ complete / partial / visit_only
completed (완료) | partial (부분완료) | visit_only (출장비만)
   ↓ cancel (어느 단계든 가능)
canceled (취소)
```

각 단계에서 가능 액션 (`STATUS_ACTIONS`):
- received → assign / cancel
- assigned → confirm / cancel
- confirmed → start / cancel
- in_progress → complete / partial / visit_only / cancel
- 완료/부분완료/출장비만/취소 = 종결

### 4-2. 새 작업 진입 흐름

#### 4-2-A. 진입 채널 (코드 분석)

| 채널 | 진입점 | 처리 위치 |
|---|---|---|
| 카톡 (해피콜) | HappycallApp NewReceptionScreen | parseKakao() 텍스트 파싱 |
| 직접 입력 (해피콜) | HappycallApp NewReceptionScreen | Field 폼 → addTask() |
| 직접 입력 (운영자) | AdminApp NewReceptionFormScreen | 5256줄~ |
| partner PWA (김쿨가이) | PrincipalApp NewTab | parseKakao() v2 → 6 필드 폼 |
| CSV 임포트 (유솔 N 발주서) | UsolNOrders / data/usolNOrderCsv.js | csvOrderRowToTask() → makeEmptyTask |
| Apps Script API | api.js `createTask()` | (실제 호출 흔적 없음) |
| 시드 (검증용) | seedTasks.js USOL_N_SEED_TASKS | localStorage 첫 로드 시 |
| 시드 (mock) | shared/tasks.js INITIAL_TASKS | TasksContext 초기값 |

#### 4-2-B. 진입 후 흐름 (현재 구현 — mock 기준)

1. `addTask(newTask)` (TasksContext) → `setTasks(prev => [newTask, ...prev])` (메모리만)
2. `happycallStatus = "uncontacted"` 시작 → 해피콜이 통화 / 메모 → `"contacted"`
3. 해피콜이 추천 기사 작성 → `recommendedEngineer = "이름"`
4. 해피콜이 기사 배정 → `assignedEngineer / assignedEngineerId` + `happycallStatus = "assigned"`
5. status = "약속대기" → 기사가 일정 잡으면 "확정"
6. 작업 시작 → "진행중", `startedAt` 시각
7. 완료 → "완료", `completedAt`, 사진 업로드 (mock: `beforePhoto/afterPhoto = true`)

#### 4-2-C. 자동 배정 (가스만 — `assignment_type` 코드)

- `manual` (기본 — 수동 배정)
- `auto_first_accept` (가스 — 일괄 푸시 → 선착순 수락)
- `manual_with_recommendation` (그 외 — 수동 + 추천)

→ AdminApp `RecommendScreen` (수동 추천) / `AutoAssignScreen` (자동 일괄 푸시) 두 화면 분기. 파일에는 박혀 있지만 실제 백엔드 연결은 mock.

`auto_first_accept` 흐름 (코드 — `getAutoBroadcastCandidates`):
1. 작업 종류 + 지역 + 기종 매칭으로 후보 N명 (max 4명)
2. 4명 동시 푸시 → 선착순 수락 → 작업 배정
3. 5분 미수락 → owner 수동 배정 알림 (docs 결정 7 — 코드 구현 확인 필요)

### 4-3. 기사 수락/거절 시스템

`db/migrations/001_init.sql` `task_assignments` 테이블:
```sql
status CHECK IN ('대기','수락','거절','마감')
notified_at / responded_at
```

→ DB는 박혀 있음. PWA UI / 흐름 구현은 **확인 필요** (RecommendScreen / AutoAssignScreen 내부 코드는 mock).

### 4-4. 일정 협의 흐름 (해피콜 역할)

코드 흐름 (HappycallApp + shared/tasks.js):

1. `happycallStatus: "uncontacted"` (전화 안 함) → 카드 빨강 강조
2. 해피콜 통화 후 → `happycallStatus: "contacted"` + `happycallMemo` 저장
3. `recommendedEngineer` 작성 (선택)
4. 기사 배정 (AssignEngineerScreen — `EngineerTimelineCard`로 일정 시각화) → `happycallStatus: "assigned"`
5. `scheduledDate / scheduledTime` 작성

**Phase 1 결정 (docs)**: 해피콜 ❌ — Phase 1은 운영자(operator)가 직접 처리. HappycallApp는 코드 유지. (코드 vs Phase 1 정책 차이)

### 4-5. 진행중 → 완료 흐름

EngineerApp `TaskDetailScreen` + `CompletionReportScreen`:

1. 기사 [작업 시작] → `state: "active"`, `startedAt: 시각`
2. 작업 진행 (사진 촬영 — beforePhoto)
3. 기사 [완료 보고] → CompletionReportScreen 진입
4. afterPhoto 업로드 (mock: boolean)
5. extra_fee 입력 (현장 추가금)
6. work_memo 입력
7. [완료 확정] → `state: "done"`, `completedAt: 시각`, status: "완료"
8. 정산 자동 계산 (compute_payment 함수 — DB 함수, **확인 필요** — 코드 호출 흔적 없음)

**V12-3 운영자 작업상세 변화**: AdminTaskDetailScreen에는 메인 "완료" 버튼 X. 기사가 완료 처리 → 자동 업데이트 (예외 처리만 운영자가).

### 4-6. 사진 업로드

- **shared/tasks.js**: `beforePhoto` / `afterPhoto` boolean (mock — 실제 업로드 X)
- **db/migrations**: `photos` 테이블 — `step / storage_path / uploaded_by / uploaded_at`. Supabase Storage 의도.
- **현재 구현**: 파일 picker만 (TaskCompleteScreen.jsx). 실제 업로드 로직 **확인 필요** (Supabase Storage 호출 코드 보이지 않음).

### 4-7. 수정 이력 (V11 / Step 11)

`data/taskHistory.js`:
- storage: `ollit_task_history_v1` (max 1000개)
- `saveTaskHistory({ taskId, action, field, oldValue, newValue, by, ... })` → unshift
- `loadTaskHistory(taskId)` → 작업별 이력
- `FIELD_LABELS` 한글 매핑 / `ACTION_ICONS` 이모지 매핑 / `formatHistoryValue()` 표기

UI: AdminApp `TaskHistoryScreen` (153줄) / 작업상세에서 [📜 수정 이력 보기] 버튼.

---

## 5. 작업 종류 4개 (세척 / 냉매 / 출장비 / 추가)

### 5-1. SERVICE_TYPES (4종 — `data/serviceTypes.js`)

| id | 라벨 | 아이콘 | 색 | 식별 조건 |
|---|---|---|---|---|
| cleaning | 세척 | ❄️ | #00BCD4 (시안) | 기본 / `workType === "냉매충전"` 아닌 모든 것 |
| refrigerant | 냉매 | ⚡ | #FFB300 (노랑) | `workType === "냉매충전"` 또는 텍스트 "냉매/가스/충전" |
| visit | 출장 | 🚗 | #555555 (그레이) | `task.status === "visit_only"` |
| extra | 추가 | ➕ | #FF8F00 (주황) | `task.orderType === "extra"` (유솔 N 추가선택) |

### 5-2. 작업 종류별 흐름 차이

#### 5-2-A. 세척 (cleaning)
- **배정**: `manual_with_recommendation` (수동 + 추천)
- **수락**: 기사가 운영자 메시지로 일정 협의 (자동 X)
- **단가**: `data/standardRates.js` `cleaning` 기준 (벽걸이 4만 / 스탠드 6만 / 4way 7만 등)
- **정산**: 원청 정책 type 따라 (standard / fake_split / naver_settlement)
- **부분완료** 가능: 작업 일부만 — 별도 status

#### 5-2-B. 냉매 / 가스 (refrigerant)
- **배정**: `auto_first_accept` (★ Phase 1 핵심 — 일괄 푸시 → 선착순)
- **수락**: 4명 동시 푸시, 5분 내 선착순 → owner 수동 fallback
- **단가**: 정책 별로 — 쿨가이 50% / 용인 정액 1만 / 크리크린 20% (견적 기준)
- **시그니처**: `task.hasRefrigerantItem === true` 또는 `workType === "냉매충전"`
- **계산**: `calcRefrigerant({ policy, estimate, extra, engineerId })` — engineer 단가 / 비율 / 정액 + override 가능

#### 5-2-C. 출장비만 (visit_only)
- **트리거**: 기사가 현장 갔는데 작업 못함 → `[🚗 출장비만 정산]` 버튼
- **사유 4개**: wrong_type / no_access / customer_absent / other
- **고정 30,000원**: 기사 100% / 회사 0 / 원청 0
- **상태 전환**: in_progress → visit_only (FINAL)
- **다이얼로그**: VisitOnlyDialog.jsx (151줄)

#### 5-2-D. 추가선택 / 추가 (extra)
- **유솔 N 전용**: `task.orderType === "extra"` (basic vs extra 자동 분류 — usolNClassify.js)
- **분류 키워드**: "실외기" / "곰팡이 살균" / "가스 충전" / "냉매 충전" / "추가"
- **분배**: `additionalsPolicy: { principalRate: 15, engineerRate: 85 }` (회사 0)
- **계산**: `calcEngineerEarning(extra task) = calcCompanyReceive(task)` (= 기사 100%)

### 5-3. 단가 정책 (`data/standardRates.js`)

```js
SEED_RATES = {
  cleaning: {                     // 진짜 기사 단가
    "벽걸이":  40000, "스탠드":  60000, "투인원": 100000,
    "1way":   50000, "4way":   70000, "원형":   80000,
  },
  coolguyFake: {                  // 쿨가이 가짜 단가 (기사에게만 노출)
    "벽걸이":  50000, "스탠드":  70000, "투인원": 110000,
    "1way":   60000, "4way":   80000, "원형":   90000,
  },
}
```

`getEngineerRate(applianceType, vatPolicy)`: vatPolicy=excluded면 ×1.10 (유솔 N).

`getCoolguyFakeRate(applianceType)`: 쿨가이만 가짜 단가 표시.

→ 차이 (가짜 - 진짜) = 사장님 마진 (정보 비대칭). owner only 노출.

(docs/원청별_워크플로우.md `BASE_PRICES`에는 다른 단가 — HappycallApp용 시뮬 단가. 운영 시는 standardRates.js + 정책 시트 참조)

### 5-4. 자동 배정 정책 (코드 분석 — `getAutoBroadcastCandidates`)

위치: `src/pages/AdminApp.jsx:704`

```js
function getAutoBroadcastCandidates(workType, region, appliance, maxCount = 4)
```

- 후보 = 권한(workType) + 지역(region) 매칭 기사
- max 4명 동시 푸시
- 우선순위: main → backup → none

→ 코드는 박혀 있지만 실제 푸시 로직은 mock (RecommendScreen / AutoAssignScreen 내부 mock). Supabase Realtime / FCM 연결 흔적 없음.

---

## 6. 원청 6개 정책

### 6-1. 코드 시드 6개 (`data/principals.js`)

| id | 회사 | 라벨 색 | 정책 type (세척) | 정책 type (냉매) | vatPolicy | note |
|---|---|---|---|---|---|---|
| `allday` | 올데이케어 | #FF1B8D | standard / 원청 0 | standard / 원청 0 / 기사 50% | included | 직영 / 원청 수수료 0 |
| `aircon_pro` | 에어컨프로 (쿨가이) | #06B6D4 | fake_split / split 50% | standard / 원청 10% / 기사 50% | included | 세척=가짜단가 / 냉매=10% |
| `yongin` | 용인컴퍼니 | #888780 | standard / 정액 10,000 | standard / 정액 10,000 / 기사 50% | included | 원청 정액 1만 |
| `usol_h` | 유솔홈케어 H | #10B981 | standard / 원청 15% | standard / 원청 0 / 기사 50% | included | 현금 결제 / 세척 15% |
| `usol_n` | 유솔홈케어 N | #03C75A | naver_settlement / 정산예정×15% | (없음) | excluded | 네이버 / CSV / 부가세 별도 |
| `crikrin` | 크리크린 | #7F77DD | standard / 원청 20% | standard / 원청 20% (견적 기준) / 기사 50% | included | 냉매=견적×20% / 세척=총×20% |

**docs vs 코드 불일치**:
- docs는 KA(쿨가이 아버지) / KB(쿨가이 아들) 분리를 박았는데, 코드 시드는 단일 `aircon_pro`만.
- docs는 6 원청에 KA/KB 따로, 총 6개 (O / KA / KB / Y / H / N — 크리크린 제외 / 세스코 영구 제외).
- 코드는 6개 (allday / aircon_pro / yongin / usol_h / usol_n / crikrin).

### 6-2. 정책 type 3종 + 8 calc_method

#### 6-2-A. standard (표준)
- `principal: { type: "rate" | "fixed" | "none", base: "estimate" | "total" | "settlement", value }`
- `engineer: { type, base, value, overrides: [] }`

#### 6-2-B. fake_split (쿨가이형)
- `fakeRates: { 기종: 단가 }` — 기사에게 노출되는 가짜 단가
- `splitRate: 50` — (총금액 - 가짜) × 50% = 원청 수수료
- 사장님 마진 = (가짜 - 실제) + (총 - 가짜) × 50%

#### 6-2-C. naver_settlement (유솔 N)
- `principal: { type: "rate", base: "settlement", value: 15 }` — 정산예정금액 × 15%
- `additionalsPolicy: { principalRate: 15, engineerRate: 85 }` — 추가선택 분배

#### 6-2-D. 8 calc_method (DB enum)

`db/migrations/001_init.sql` `commission_policies.calc_method`:
1. `비율` — 표준 비율 (예: 원청 15%)
2. `차감후비율` — (총 - 기사) × 비율 (쿨가이 세척)
3. `금액` — 정액 (예: 용인 1만)
4. `수량비율` — 수량별 비율 (유솔 N 세척)
5. `AG전체_AD반반` — 기사 전체 / 사장님 AD 반반 (쿨가이 가스 일부)
6. `예상금액비율` — 예상금액 비율 (쿨가이 가스 — 10/50/40 또는 35/50/15)
7. `비율_총액기준` — 합계 기준 (쿨가이 1way/4way/원형 2개+)
8. `추가선택` — 유솔 N 추가선택 (85/15/0)

(코드 `commissionCalc.js`는 standard / fake_split / naver_settlement 3 type만 구현. 8 calc_method 매핑은 SQL 함수 `compute_payment` 의도 — 미구현)

### 6-3. 원청별 접수 흐름 / 정산 시점 / 부가세 / 특이사항

| 원청 | 접수 흐름 | 정산 시점 | 부가세 | 특이사항 |
|---|---|---|---|---|
| 올데이케어 | 사장님/영업/카톡/전화 → operator 입력 | 매일 22시 마감 | 포함 | 자체 영업 / 마진 100% / 원청 수수료 0 |
| 쿨가이 (KA/KB) | partner PWA / 카톡 | 매일 22시 마감 | 포함 | 가짜 단가 (정보 비대칭) / 가스 자동 배정 |
| 용인컴퍼니 | 외주 카톡 / 전화 → operator | 매일 22시 마감 | 포함 | 원청 정액 10,000 |
| 유솔홈케어 H | 외부 시트 자동 동기화 / 카톡 | 매일 22시 마감 | 포함 | 현금 결제 / 세척 15% |
| 유솔홈케어 N | 외부 시트 (자동) / CSV 업로드 | **매주 월요일 + 매월 15일** | **별도** (×1.10) | 네이버 결제 / 추가선택 85/15 / 3시점 추적 |
| 크리크린 | 시트 / 카톡 | 매일 22시 마감 | 포함 | 냉매=견적 / 세척=총 / 20% 일관 |

**유솔 N 3시점 추적** (`data/tasks.js` `getUsolNStatus`):
- pending_settlement (🟡) — `naverSettledAt` 없음
- pending_deposit (🟠) — `naverSettledAt` 있음, `companyReceivedAt` 없음
- completed (✅) — 둘 다

**13일 경고**: `getLongPendingTasks(13)` — 작업 완료 후 13일+ 정산 안 된 유솔 N 작업.

---

## 7. 정산 흐름 (4단계)

### 7-1. 4단계 흐름

```
고객 → 원청 → 회사 → 기사
```

#### 7-1-A. 5원청 (올데이/쿨가이/용인/유솔H/크리크린) — 매일 마감

코드 흐름 (`data/dailyClose.js`):

1. **고객 → 기사 (현장)** — 기사가 직접 받음 (현금/카드)
2. **기사 → 회사** — 매일 22시까지 입금. `engineerPaidAt` 시각
3. **회사 → 기사** — `engineerEarning` 미수령분 송금 (역방향 — 기사 단가가 받은 돈보다 많을 때, 보통 회사가 받음)
4. **회사 → 원청** — 별도 (월별 또는 정해진 주기 — 코드 미구현)

`pickAmountFromEngineer(t)`:
```js
amount = (estimateTotal + addonFee) - engineerEarning   // 회사 받을 돈
```

`SettlementScreen.jsx`:
- TopStats: "오늘 받을 돈" / "미입금 N명" / "입금 완료" (V12-3 단색 박스)
- TodayMarginSection: 회사 마진 분석
- FilterTabs: 미입금 / 전체 / 입금 완료
- EngineerSettlementCard 리스트
- UnpaidAlert: 단색 빨강 박스 (V12-3)

#### 7-1-B. 유솔 N — 별도 흐름

```
고객 → 네이버 결제 (paidAt)
   ↓ 보통 7-13일
유솔 → 네이버 정산 (naverSettledAt)
   ↓ 매주 월요일
유솔 → 회사 (companyReceivedAt) — 정산예정금액 × 85%
   ↓ 매월 15일
회사 → 기사 (engineerSettledAt)
```

화면: `UsolNTracking.jsx` / `UsolNEngineerSettlement.jsx`

### 7-2. 매일 22시 마감 시스템

`utils/dailyAlertScheduler.js`:

- `ALERT_HOUR = 22`, `ALERT_MINUTE = 0`
- `LAST_ALERT_KEY = "ollit_last_alert_date"` (오늘 이미 발송했는지 체크)
- 동작: 브라우저 `setTimeout`으로 다음 22시까지 예약 → `sendDailyAlerts()` 호출
- 탭 전환 시 누락 체크 (`visibilitychange` 핸들러)
- 발송: `getUnpaidByEngineer()` → `notificationQueue`에 push + Notification API (권한 있을 때)
- 수동 발송: `sendManualAlerts()` (운영자 [일괄 알림] 버튼)

**주의**: 브라우저 열려 있을 때만 작동. PWA가 닫혀 있으면 누락 → 다음 오픈 시 catch up.

### 7-3. "오늘 받을 돈" / "오늘 번 돈" / "오늘 마진" 계산

`SettlementScreen.jsx` TopStats:
- "오늘 받을 돈" (expected) = 매일 마감 5원청 완료된 작업의 회사 받을 돈 합계
- "미입금 N명 (받을 돈)" = expected - received
- "입금 완료 (받은 돈)" = received

`utils/marginCalculator.js`:
- 작업별: `analyzeTaskMargin(task)` → `{ revenue, engineerEarning, margin, principalName, principalColor }`
- 유솔 N 분기: `calcCompanyReceive` / `calcEngineerEarning` / `calcCompanyMargin`
- 5원청: `revenue = totalAmount`, `margin = revenue - engineerEarning - principalFee`
- 합계: `calculateMarginByTask(tasks)` → totalRevenue / totalEngineerEarning / totalMargin / byPrincipal[]
- 기간별: `calculateMarginByPeriod(tasks, "today" | "week" | "month")`

`utils/dashboardStats.js`:
- `computeDashboardStats({ tasksToday, newReceptions, extraReceptions, assignedTasks, user })`
- 출력: `{ new, assigned, confirmed, inProgress, completed, revenue: { total, principal, engineer, margin?, fakeEngineerTotal? }, completedTasks, unassigned, unassignedCount }`
- `revenue.margin` — owner / admin only
- `revenue.fakeEngineerTotal` — owner only (쿨가이 가짜 단가 합계)

### 7-4. 미입금 비상 시스템

위치: `SettlementScreen.jsx` UnpaidAlert (V12-3 단색 빨강 박스)

```jsx
🚨 오늘 미입금 N명 / 총 ₩금액
매일 22시까지 자동 알림 발송 · 통화 또는 직접 입금 처리 가능
```

- 일일 알림: dailyAlertScheduler (22시)
- 중복 방지: 같은 id (`unpaid_{date}_{engineerId}`) 큐에 있으면 추가 X
- 미입금 1일+ → 카드에 "미입금 N일" Chip 패턴 배지

### 7-5. payments 결과 SQL (db/migrations)

```sql
status: 미정산 → 사장님입금 → 기사확인 → 정산완료 (+ 보류/분쟁/지연)
paid_at: 사장님 [입금 완료] 클릭 시각
confirmed_at: 기사 [입금 확인] 시각 (원청 정산은 NULL)
```

→ 양방향 확인 (사장님 → 기사) 흐름이 DB에는 박혀 있음. PWA UI 구현은 **확인 필요**.

---

## 8. 자동화 시스템

### 8-1. Google Sheets 동기화

**현재 상태**: `src/api.js`에 Apps Script URL 1개 박힘. `getTasks(role, userId, principalCode)` 함수 존재. PWA에서 import는 `HappycallApp.jsx:2` 1곳만 (실 호출 빈도 확인 필요).

**확인 필요**: docs/시트_vs_운영DB_갭.md / docs/유솔_네이버_상세.md 참조. 코드만 봐서는 동기화 메커니즘 미파악.

### 8-2. Google Calendar 자동 (공용 + 기사 22개)

**코드 흔적**: 없음. App.jsx / pages / utils 어디에도 Google Calendar API 호출 없음. lucide-react `Calendar` 아이콘은 EngineerApp 내 자체 캘린더 UI(`CalendarScreen` / `DayDetailScreen`)에 사용.

**확인 필요**: docs/Phase별_로드맵.md `Phase 1` 결정 — `외부 의존 0 (캘린더 X, 텔레그램 X, SMS X)`. 캘린더 자동화는 docs에 의도만 있고 코드 X.

### 8-3. Apps Script 트리거

**확인 필요**: api.js의 Apps Script URL은 deployed function. 그 안의 트리거 (onEdit / 매시간 / 신규접수) 코드는 PWA 저장소 외부 (Apps Script 콘솔). 이 저장소만 봐서는 알 수 없음.

### 8-4. 매시간 자동 실행 / onEdit 트리거

**확인 필요** — 위와 동일. PWA 저장소 외부.

### 8-5. PWA 내 자동화 (확인된 것)

| 자동화 | 위치 | 동작 |
|---|---|---|
| 매일 22시 미입금 알림 | utils/dailyAlertScheduler.js | 브라우저 setTimeout, 22시 발송 |
| 가스 자동 배정 (mock) | AdminApp `getAutoBroadcastCandidates` | 권한+지역 매칭 후보 4명 (UI만) |
| 유솔 N CSV 자동 매칭 | utils/usolNAutoMatch.js | productOrderId로 작업DB 매칭 |
| 유솔 N 추가선택 자동 분류 | data/usolNClassify.js | 키워드 기반 basic/extra 자동 |
| 유솔 N 13일+ 정산 대기 경고 | data/tasks.js `getLongPendingTasks` | 작업 완료 13일+ → 경고 |
| 정보 비대칭 (쿨가이 가짜 단가) | data/standardRates.js | 기사 단가 vs 쿨가이 단가 분리 |
| 자동 추천 기사 점수 | utils/engineerRecommendation.js | 지역 40 + 기종 20 + 일정 20 + 활동 10 + 등급 10 |

---

## 9. 화면 전체 목록

### 9-1. AdminApp (운영자 / admin / lee.ceo) — 5942줄

#### 9-1-A. 메인 + 진입

- `App.jsx` → `AdminApp({ user, onLogout })`
- 상단: 모드 토글 (다크/라이트), 로그아웃 버튼
- DashboardScreen (1940줄) — `dashboardActiveTab` 4탭

#### 9-1-B. 대시보드 4탭

| 탭 | 컴포넌트 | 라인 | 내용 |
|---|---|---|---|
| overview | OverviewTab | 2087 | 통계 + 새 접수 + LiveWork |
| tasks | (확인 필요) | — | — |
| engineers | EngineersTab | 2173 | 기사 목록 + 카드 |
| activities | (확인 필요) | — | — |

#### 9-1-C. 화면 전체 (31개)

(위 3-2-C 표 참조)

#### 9-1-D. 메뉴 / 모달 / 다이얼로그

- TaskCardMenu (⋯ 메뉴) — 단계별 액션 (상세/통화/메모/수정/취소 등)
- MemoBottomSheet — 메모 즉석 추가
- TaskCancelDialog — 취소 4사유
- VisitOnlyDialog — 출장비만 4사유
- AllEngineersModal — 전체 기사 목록 모달
- ToastContainer — 토스트 알림

#### 9-1-E. 사용 권한 / 데이터 / 액션

- 모든 task / 모든 engineer / 모든 principal 보기
- 배정 / 취소 / 완료 / 가격 편집 가능
- 회사 마진 / 가짜 단가 보기 (admin = lee.ceo만 owner로 승격)
- 정산 전체 / 단가 관리 / 사용자 관리 / 백업 (owner only)

### 9-2. HappycallApp (해피콜 / happycall) — 2193줄

| screen | 컴포넌트 | 라인 | 내용 |
|---|---|---|---|
| main | HappycallMainScreen | 370 | ActionAlert + 탭(전체/미배정/배정대기/완료) + TaskCard 리스트 |
| newReception | NewReceptionScreen | 788 | 새 접수 (카톡 paste / 직접) — CustomDatePicker 포함 |
| edit | HappycallEditScreen | 1240 | 작업 수정 |
| assign | AssignEngineerScreen | 1524 | 기사 배정 — TimelineHeader + EngineerTimelineCard |
| memo | MemoEditScreen | 2054 | 해피콜 메모 |

**기사 데이터**: 자체 mock 5명 (E001~E005, 일정 풀 캘린더 포함, 주기적 휴무).

### 9-3. EngineerApp (기사 / engineer) — 3237줄

#### 9-3-A. 4 탭 (BottomTabBar)

| 탭 | 컴포넌트 | 라인 |
|---|---|---|
| today (오늘) | MainScreen | 500 |
| settlement (정산) | SettlementScreen | 1288 |
| notifications (알림) | NotificationsScreen | 1578 |
| profile (프로필) | ProfileScreen | 1762 |

#### 9-3-B. 내부 화면

- TaskDetailScreen (710) — 작업 상세 / 시작/완료
- CompletionReportScreen (1118) — 완료 보고 + 사진 + extra_fee
- CalendarScreen (2155) — 월/주 캘린더
- DayDetailScreen (2419) — 날짜별 작업 + 휴무
- TimeWheelSheet (2796) / DragTimePicker (2974) / AddOffDayModal (3014) — 시간 picker

#### 9-3-C. 내부 컴포넌트

- ActionAlert (612) / CompactTaskCard (654)
- JobSettlementCard (1497) / SmallMetric (1558)
- NotificationItem (1678) / ProfileMenuItem (2028) / ProfileStat (2007)
- BottomTabBar (2055)
- Section (1240) / InfoBox (1252) / Row (1261) / PhotoUploadBox (1270)
- CustomDatePicker (261) / CustomTimePicker (386)

### 9-4. PrincipalApp (원청 / principal / 김쿨가이) — 1416줄

| 탭 | 컴포넌트 | 라인 |
|---|---|---|
| list | ListTab | 853 |
| new | NewTab + parseKakao | 489 (133 parseKakao) |
| settle | SettleTab | 1176 |
| info | InfoTab | 1358 |

**내부 화면**:
- TaskDetail (998)
- SubmittedScreen (778)

**parseKakao 로직** (PrincipalApp.jsx:133): 카톡 텍스트 파싱 — 이름 / 전화번호 / 주소 / 가격 자동 추출. partner-safe (자기 의뢰만 등록).

### 9-5. LoginScreen — 두 버전

| 파일 | 라인 | 사용 |
|---|---|---|
| `src/pages/LoginScreen.jsx` | 892 | (App.jsx import 안 됨) — 시안 v4 / 4단계 step |
| `src/components/LoginScreen.jsx` | 154 | App.jsx에서 사용 — 단순 입력 + 빠른 로그인 |

**확인 필요**: pages/LoginScreen.jsx는 코드에 박혀 있지만 사용 안 됨 (App.jsx는 components/LoginScreen 사용). 미정리 자산.

### 9-6. 공통 컴포넌트 (`src/components/`)

| 컴포넌트 | 용도 |
|---|---|
| Chip.jsx (V12-3 신규) | 표준 칩 — 흰배경 + 보더 + 활성 시 진하게 |
| EngineerBadge.jsx | 기사 이름 + 직급 표기 (sm/md/lg) |
| ServiceTypeBadge.jsx | 작업 종류 칩 (V12-3 흰배경) |
| OllitMark.jsx / OllitLoader.jsx | 로고 / 로딩 |
| SplashScreen.jsx | 첫 1.5초 스플래시 |
| TaskCardMenu.jsx | ⋯ 메뉴 (단계별 액션) |
| AdminTaskDetailScreen.jsx (V12-3 신규) | 운영자 작업상세 재설계 (영역 6 + 예외 처리 접힘) |
| SettlementScreen.jsx | 정산 (5원청 매일 마감) |
| PrincipalSettlementScreen.jsx | 회사→원청 정산 |
| EngineerSettlementScreen.jsx | 기사 본인 정산 |
| UsolNScreen.jsx + 5탭 | 유솔 N 워크스페이스 |
| TaskCompleteScreen / TaskEditScreen / TaskHistoryScreen | 작업 처리 |
| MemoAddScreen / VisitOnlyDialog | 모달 / 다이얼로그 |
| EngineerListScreen / EngineerEditScreen | 기사 관리 |
| PrincipalListScreen / PrincipalEditScreen / PrincipalDetailScreen | 원청 관리 |
| RegionListScreen / RegionEditScreen / RegionEngineerSearch | 지역 관리 |
| UserListScreen / UserEditScreen | 사용자 관리 |
| RatesManagementScreen | 단가 관리 |
| NaverUploadScreen | 네이버 CSV 업로드 |
| AllEngineersModal | 전체 기사 모달 |
| NotificationsScreen | 알림 |
| SettingsScreen | 설정 / 메뉴 진입 |

### 9-7. 기사 PWA / 원청 PWA 현재 상태

- **기사 PWA** = EngineerApp.jsx (작동 중 — mock 데이터). 디자인은 V11-11까지 적용. V12-3 미적용 (Phase 1B 진입 예정).
- **원청 PWA** = PrincipalApp.jsx (작동 중 — mock 데이터). 김쿨가이만 시드. V12-3 미적용.
- **기사 PWA Phase 1B 시작 준비** (V12-3 보고서 마지막 메모).

---

## 10. 디자인 시스템 (V12-3 적용 후)

### 10-1. themes.js 변수 전체 (`src/styles/themes.js`)

#### 10-1-A. DARK_THEME

```js
"--bg-primary":       "#1A1512",
"--bg-secondary":     "#221C18",
"--bg-tertiary":      "#2A2420",
"--bg-quaternary":    "#322A24",
"--bg-inset":         "#0F0B0A",
"--border-strong":    "#3A322C",
"--text-primary":     "#FAF8F5",
"--text-secondary":   "#888780",
"--text-tertiary":    "#555",
"--accent":           "#FF1B8D",
"--accent-bg":        "rgba(255,27,141,0.10)",
"--border":           "#2A2420",
// V12-2 색 시스템
"--success":          "#00875A",
"--success-bg":       "rgba(0,135,90,0.12)",
"--warning":          "#FF8F00",
"--danger":           "#FF3D5A",
// status alias
"--status-success":   "#00875A",
"--status-warning":   "#FF8F00",
"--status-info":      "#00BCD4",
"--status-danger":    "#FF3D5A",
"--accent-soft":      "rgba(255,27,141,0.10)",
// 작업 종류 색
"--service-cleaning":    "#00BCD4",
"--service-refrigerant": "#FFB300",
"--service-visit":       "#888888",
"--service-extra":       "#FF8F00",
// 기사 직급 (V11-11 — 핑크 단순화: 베테랑만 강조)
"--engineer-rookie":  "#888780",
"--engineer-career":  "#888780",
"--engineer-expert":  "#FF1B8D",
// 유솔 N 전용 (다크)
"--usol-n-content-bg":      "#221C18",
"--usol-n-card-bg":         "#2A2420",
"--usol-n-border":          "#3A322C",
"--usol-n-shadow":          "none",
"--usol-n-shadow-hover":    "0 4px 12px rgba(0,0,0,0.3)",
"--usol-n-highlight-shadow":"0 4px 12px rgba(3,199,90,0.20)",
```

#### 10-1-B. LIGHT_THEME

```js
"--bg-primary":       "#FFFFFF",
"--bg-secondary":     "#FFFFFF",
"--bg-tertiary":      "#FAF8F5",
"--bg-quaternary":    "#F5F2ED",
"--bg-inset":         "#F5F2ED",
"--text-primary":     "#1A1512",
"--text-secondary":   "#6B6862",
"--text-tertiary":    "#9B9892",
"--accent":           "#FF1B8D",
"--accent-bg":        "rgba(255,27,141,0.06)",
"--border":           "#EBE7DF",
"--border-strong":    "#DDD7CD",
// 색 시스템 (다크와 동일)
"--success":          "#00875A",
"--success-bg":       "rgba(0,135,90,0.08)",
"--warning":          "#FF8F00",
"--danger":           "#FF3D5A",
"--status-success":   "#00875A",
"--status-warning":   "#FF8F00",
"--status-info":      "#00BCD4",
"--status-danger":    "#FF3D5A",
"--accent-soft":      "rgba(255,27,141,0.08)",
// 작업 종류
"--service-cleaning":    "#00BCD4",
"--service-refrigerant": "#FFB300",
"--service-visit":       "#555555",
"--service-extra":       "#FF8F00",
// 기사 직급
"--engineer-rookie":  "#999",
"--engineer-career":  "#666",
"--engineer-expert":  "#FF1B8D",
// 유솔 N 전용 (라이트)
"--usol-n-content-bg":      "#FAF8F5",
"--usol-n-card-bg":         "#FFFFFF",
"--usol-n-border":          "#EBE7DF",
"--usol-n-shadow":          "0 1px 3px rgba(0,0,0,0.04)",
"--usol-n-shadow-hover":    "0 4px 8px rgba(0,0,0,0.08)",
"--usol-n-highlight-shadow":"0 4px 12px rgba(3,199,90,0.12)",
```

#### 10-1-C. 적용 함수

```js
applyTheme(theme) {
  Object.entries(colors).forEach([k, v] => document.documentElement.style.setProperty(k, v));
  document.body.style.background = colors["--bg-primary"];
  document.documentElement.style.colorScheme = theme === "light" ? "light" : "dark";
  localStorage.setItem("ollit_theme", theme);
}
loadTheme() {
  return localStorage.getItem("ollit_theme") || "dark";   // 기본 다크
}
```

#### 10-1-D. index.css `:root` 변수

```css
:root {
  --accent-bg:           rgba(255,27,141,0.06);
  --accent-bg-strong:    rgba(255,27,141,0.12);
  --accent-border:       rgba(255,27,141,0.30);
  --accent-border-strong:rgba(255,27,141,0.50);
  --accent-gradient:     linear-gradient(135deg, #FF1B8D, #FF1B8D);  /* V12-3 통일 */
  --accent-gradient-soft:linear-gradient(135deg, rgba(255,27,141,0.12), rgba(255,27,141,0.02));
  --naver-gradient:      linear-gradient(135deg, #03C75A, #02A847);
}
[data-theme="dark"] {  /* 다크 시 더 진하게 */
  --accent-bg:           rgba(255,27,141,0.08);
  --accent-bg-strong:    rgba(255,27,141,0.16);
  --accent-border:       rgba(255,27,141,0.40);
  --accent-border-strong:rgba(255,27,141,0.60);
}
```

### 10-2. 색깔 시스템 (V12-3 적용 후)

#### 10-2-A. 핫핑크 통일 (#FF1B8D)

- 모든 강조면 (오늘 받을 돈 / 오늘 마진 / 입금 완료 / 새 접수 등록 등) = 단색 #FF1B8D
- 다른 핑크 (#E91E80 / #D81B60 / #C2185B / #EC407A / #FF0080) = 0건 (V12-3 통일 완료)
- 옅은 핑크 배경 (rgba(255,27,141,0.X)) = 라이브 컴포넌트 0건 (UsolN/SettingsScreen 그라데이션 예외)

**예외 (유지)**:
- SettingsScreen.jsx:208 — `linear-gradient(135deg, rgba(3,199,90,0.08), rgba(255,27,141,0.02))` (그린-핑크 브랜드 그라데이션)
- usol_n/UsolNCsvMatch.jsx:120 / :148 — 핑크-퍼플 그라데이션 (유솔 N 정체성 유지)
- shared/users.js (REGISTERED_USERS) — admin role color `#E91860` (다른 핑크 / 코드 외부 데이터)

#### 10-2-B. 상태 / 신호 색

| 용도 | 색 | CSS 변수 |
|---|---|---|
| 진행중 / 진행 강조 | #FF1B8D | --accent |
| 완료 / 성공 | #00875A | --success / --status-success |
| 미입금 / 위험 / 취소 | #FF3D5A | --danger / --status-danger |
| 주의 / 이동 | #FF8F00 | --warning / --status-warning |
| 정보 / 시안 | #00BCD4 | --status-info / --service-cleaning |
| 냉매 노랑 | #FFB300 | --service-refrigerant |
| 출장 그레이 | #555555 / #888888 | --service-visit |
| 추가 주황 | #FF8F00 | --service-extra |
| 베테랑 강조 | #FF1B8D | --engineer-expert |
| 신입/경력 무채 | #888780 / #666 / #999 | --engineer-rookie / -career |

### 10-3. Chip 컴포넌트 (V12-3 신규)

`src/components/Chip.jsx`:

```jsx
<Chip
  icon="❄️"
  label="세척"
  color="#00BCD4"      // 활성 시 보더/글자 색
  active={true}        // 2px 컬러 보더 + 컬러 글자 + 700
  disabled={false}
  onClick={...}
  size="md"            // sm / md / lg
/>
```

사이즈:
- sm: padding 2/6, fontSize 9
- md: padding 4/10, fontSize 11
- lg: padding 6/14, fontSize 12, borderRadius 24

기본 스타일:
- background: var(--bg-secondary) — 흰배경
- border: 1px solid var(--border) — 비활성 / 2px solid color — 활성
- color: var(--text-secondary) — 비활성 / color — 활성
- fontWeight: 500 — 비활성 / 700 — 활성

### 10-4. 카드 패턴

#### 10-4-A. 진행중 카드 (V12-3)

```jsx
<div style={{
  background: bg-secondary,
  border: 1px solid border,
  borderLeft: 4px solid #FF1B8D,        // 좌측 핑크바
  borderRadius: 10,
  position: "relative",
  // 그림자 X
}}>
  <div style={{
    position: "absolute", top: 8, right: 8,
    background: #FF1B8D, color: #fff,
    padding: "2px 8px", borderRadius: 20,
    fontSize: 9, fontWeight: 700,
  }}>
    진행중
  </div>
  ...
</div>
```

state별 색:
- active → #FF1B8D (핑크)
- moving → #FF8F00 (주황)
- waiting → text-secondary
- scheduled → text-primary
- done → #00875A (그린)
- external → #FF8F00

#### 10-4-B. 강조 박스 (V12-3 단색)

- 흰 글자 + 단색 배경 (`#FF1B8D` / `#00875A` / `#FF3D5A`)
- borderRadius: 14
- padding: 16
- 라벨 opacity: 0.95
- 부가 정보 opacity: 0.85

#### 10-4-C. 일반 카드

- background: bg-secondary
- border: 1px solid border
- borderRadius: 10/12

#### 10-4-D. 좌측 컬러바 카드 (강조)

- borderLeft: 4px solid color (그 외는 1px solid border)

### 10-5. 사장님 catch 적용 / 미적용

**적용된 catch (코드 박힘)**:
- ✅ 색이 많으면 별로 / 톤 통일 (V12-3 #FF1B8D 통일)
- ✅ 진행중 카드 = 좌측 핑크바 + 알약 (V12-3)
- ✅ 강조 박스 = 단색 (흰 글자) (V12-3)
- ✅ 칩 = 흰배경 + 보더 (V12-3)
- ✅ 운영자 작업상세 = 메인 "완료" 버튼 X (V12-3 AdminTaskDetailScreen)
- ✅ 예외 처리 = 접힘 (V12-3)
- ✅ 유솔 N 그라데이션 유지 (예외)
- ✅ 베테랑만 핑크 강조 (V11-11)
- ✅ 두 번 일 안 하기 (`makeEmptyTask` 단일 진실 소스)
- ✅ 정보 비대칭 (쿨가이 가짜/실제 단가 분리)
- ✅ 출장비 30,000원 / 기사 100% 고정
- ✅ 추가선택 기사 100% (유솔 N)
- ✅ 부가세 자동 (vatPolicy excluded → ×1.10)
- ✅ 단가 화면 편집 (RatesManagementScreen)

**미적용 / 미정리 catch**:
- ⚠️ 기사 PWA (EngineerApp) V12-3 미적용 — Phase 1B 예정
- ⚠️ 원청 PWA (PrincipalApp) V12-3 미적용
- ⚠️ KA / KB 분리 (코드는 단일 aircon_pro)
- ⚠️ Supabase 연결 (코드 박혀 있지만 import 안 됨 — Phase 1 Day 1~ 진행 중)
- ⚠️ 8 calc_method 모두 (코드는 3 type만)
- ⚠️ 유솔 N 자동 동기화 (CSV 수동 업로드 / Apps Script 코드 외부)
- ⚠️ Google Calendar 자동 (Phase 1 외부 의존 0 결정 — 코드 X)
- ⚠️ payments 양방향 확인 흐름 (DB는 박혀 있음 / PWA UI 확인 필요)
- ⚠️ 가스 자동 배정 실제 작동 (UI mock만 / Realtime 미연결)
- ⚠️ 사진 실제 업로드 (Storage 호출 코드 미발견)

---

## 11. V11~V12 시리즈 적용 상태

코드 grep 결과 — 코드 내 V-시리즈 흔적 (주석/변수/파일명):

### 11-1. V11 시리즈 (V11-1 ~ V11-11)

| 버전 | 적용 영역 | 코드 흔적 |
|---|---|---|
| V11-1 | 유솔 N 통합 — tasks.js / seedTasks.js / usolNClassify.js / usolNOrderCsv.js / usolNSettlementCsv.js / usolNCommission.js / usolNAutoMatch.js | 11곳 주석 |
| V11-2 / V11-2-fix | 메뉴 구조 — menuStructure.js / 유솔 N 5탭 단일 메뉴 | 3곳 |
| V11-3 | 미입금 비상 박스 (index.css ollit-pulse 애니메이션) | 1곳 |
| V11-4 | 매일 22시 알림 — dailyAlertScheduler.js / notificationQueue.js | 4곳 |
| V11-5 | 시드 구조 — engineers.js / regions.js getRegionGroups | 2곳 |
| V11-6 | 작업별 마진 — marginCalculator.js | 1곳 |
| V11-7 | 확인 필요 — backup 파일에만 흔적 |
| V11-8 | 유솔 N 워크스페이스 — UsolNScreen.jsx (그라데이션 헤더 + 5탭) | 2곳 |
| V11-9 | 작업 종류 배지 — ServiceTypeBadge.jsx + serviceTypes.js | 1곳 |
| V11-10 | 추천 기사 — engineerRecommendation.js (모든 활성 기사 점수) | 1곳 |
| V11-11 | 핑크 단순화 — 베테랑만 #FF1B8D, 신입/경력은 무채색 | 4곳 |

### 11-2. V12 시리즈

| 버전 | 적용 영역 | 코드 흔적 |
|---|---|---|
| V12-1 | 라이트 흰색 메인 배경 / 베이지는 강조 박스용 | themes.js / backup |
| V12-2 | 색 시스템 통일 (라이트=다크 동일 / 밝게) — themes.js / serviceTypes.js | 2곳 |
| V12-3 | (이번 세션) 강조 박스 단색 / Chip 흰배경 / 진행중 카드 좌측바 + 알약 / 운영자 작업상세 재설계 / 핑크 통일 | Chip.jsx, AdminTaskDetailScreen.jsx 신규 + 13개 파일 수정 |

### 11-3. B1 / B2 / B3 (기사 PWA)

**확인 필요**: 코드 grep 결과 = 없음. 사장님 메모에 박힌 표기 같음. 기사 PWA Phase 1B 진입 예정 — V12-3 끝에 메모됨.

### 11-4. 사장님 catch 박혀 있는데 적용 안 된 것

- `KA / KB 분리`: docs 결정사항 / 시드 박혀 있는데 코드 시드는 단일 `aircon_pro`만
- `세스코 영구 제외`: docs 결정 — 코드 시드에 없음 (이미 제외 완료)
- `해피콜 ❌ Phase 1` 결정: HappycallApp.jsx 코드 유지 (1116줄 MemoEditScreen 등) — 사실상 안 쓰이지만 남아 있음
- `유솔 N 자동 동기화`: Phase 2 — 현재는 CSV 수동 업로드 (NaverUploadScreen)
- `로그인 = 전화 + PIN`: docs 결정 — 코드는 ID + PW (REGISTERED_USERS의 password 필드)
- `1M token`: 그릇 v5 — coderbase 영향 X

---

## 12. 작동 안 하는 / 미완성 / 버그

### 12-1. 콘솔 에러 / 경고

코드 grep 결과:
- `console.error` 14곳 (대부분 try/catch — localStorage 실패, JSON 파싱 실패 등)
- `console.warn` 3곳 (Notification 발송 실패 / scheduler 로그)
- `console.log` 2곳 (Scheduler 시간 로그 — 정상)

### 12-2. 작동 안 하는 / mock 기능

| 기능 | 상태 | 위치 |
|---|---|---|
| Supabase 연결 | 코드 박힘 / 호출 X | src/lib/supabase.js |
| Google Apps Script 호출 | api.js 정의 / HappycallApp만 import | src/api.js |
| Google Calendar 동기화 | 코드 X | (없음) |
| 가스 자동 배정 실제 푸시 | UI mock만 | AdminApp `getAutoBroadcastCandidates` |
| 기사 수락/거절 응답 | DB 테이블만 / UI 미연결 | task_assignments |
| 사진 실제 업로드 | boolean / file picker만 | TaskCompleteScreen |
| 8 calc_method 함수 | 3 type만 구현 | utils/commissionCalc.js |
| 유솔 N 매주 월요일 자동 입금 처리 | 수동 / 시드 | data/tasks.js companyReceivedAt |
| payments 테이블 PWA 연결 | DB 박힘 / PWA 미연결 | (없음) |

### 12-3. mock 데이터 / 임시 코드

- `shared/tasks.js INITIAL_TASKS` — 12건 mock (TasksContext 초기값)
- `data/seedTasks.js USOL_N_SEED_TASKS` — 5건 (유솔 N 검증용)
- `AdminApp TODAY_STATS` — 메인 통계 mock
- `AdminApp TODAY_DATE = "2026-04-27"` — 비교용 고정 날짜
- `AdminApp NOTIFICATIONS_MOCK` — 알림 mock
- `HappycallApp ENGINEERS` — 5명 mock (data/engineers.js와 별개)
- 두 LoginScreen 공존 — pages/ 사용 X

### 12-4. TODO 주석

코드 grep 결과: TODO/FIXME/XXX/HACK = 0건 (한국어 "임시" 주석 다수 — placeholder UI 텍스트 / 시드 임시 라벨 등 정상).

### 12-5. 임시 비활성화된 기능

- HappycallApp MemoEditScreen (Phase 1 해피콜 X 결정) — 코드 유지, 진입 가능
- pages/LoginScreen.jsx (App.jsx import 안 됨) — 사용 X

### 12-6. 미정리 자산 (백업 파일)

git status 미커밋 파일 / 작업 디렉토리:
- AdminApp.jsx 백업 30+ 개
- HappycallApp.jsx 백업 8개
- 기타 컴포넌트 백업 다수
- src/pages/picker_new.txt / timeline_v2~v4.txt
- 루트 __day1_ping.mjs / __test_adapter.mjs / __v11_*_check.mjs 검증 스크립트

---

## 13. 알려진 이슈 (사장님 메모 / docs)

### 13-1. 구 시스템 캘린더 ~811건

**확인 필요**: 코드 / docs grep = "811" 검색 결과 없음. 사장님 외부 시트 (운영 시트 50열, 약 900행 — Phase 1 결정 #11 통합 검토 대상). docs/시트_vs_운영DB_갭.md에 관련 내용 있을 가능성.

### 13-2. V열 이상 데이터 3건

**확인 필요**: 코드 X. 사장님 운영 시트의 특정 셀 이슈로 추정.

### 13-3. 코드 분석 발견 리스크

| 리스크 | 위치 | 설명 |
|---|---|---|
| 두 user 시스템 공존 | shared/users.js + data/users.js | 4역할 / 5역할 / SQL 5역할 명칭 모두 다름 — 혼란 위험 |
| 두 task status 시스템 | shared/tasks.js (한글) + data/tasks.js (영문) + AdminApp state (영문 별개) | 통합 필요 |
| 두 LoginScreen | pages/LoginScreen.jsx + components/LoginScreen.jsx | 사용 X 자산 정리 필요 |
| Supabase throw 위험 | src/lib/supabase.js | env 미설정 시 throw → 진입 페이지에서 import 시 앱 죽음. 현재는 import 안 함 |
| Apps Script URL 노출 | src/api.js:2 | 클라이언트 코드에 박혀 있음 — 누구나 호출 가능 |
| 30+ 백업 파일 | src/pages/AdminApp.jsx.backup_* | git status 부담 / 추후 정리 필요 |
| `engineers.js` 시드 21명 vs docs 명단 28명 | data/engineers.js | 명단 동기화 필요 (E022/E023/E024/A001 등 docs 박힘) |
| `principals.js` 시드 6 vs docs KA/KB 분리 7 | data/principals.js | KA/KB 분리 미반영 |
| `dailyAlertScheduler` 브라우저 의존 | utils/dailyAlertScheduler.js | PWA 닫혀 있으면 누락 (visibilitychange catchup만) |
| INITIAL_TASKS 12건 / 날짜 고정 (2026-04-27) | shared/tasks.js | 시연용 / 운영 시 동적 데이터 필요 |
| EngineerApp ENGINEERS / HappycallApp ENGINEERS | EngineerApp 자체 정의 | data/engineers.js와 별개 5명 (E001~E005) |
| `iso 파싱` 실패 시 fallback X | data/usolNClassify.js parseDate | NaN → null 반환 (UI 측 처리 확인 필요) |

### 13-4. docs vs 코드 핵심 불일치

- `KA / KB 분리`: docs 박힘 / 코드 단일
- `5역할 owner/admin/happycall/engineer/principal`: data/users.js 박힘 / App.jsx는 4역할 (owner = admin 중 lee.ceo)
- `partner role`: SQL 박힘 / 코드 `principal`
- `전화 + PIN 인증`: docs 박힘 / 코드 ID + PW
- `8 calc_method`: SQL 박힘 / 코드 3 type
- `Phase 1 해피콜 ❌`: docs 박힘 / 코드 HappycallApp 유지
- `외부 의존 0 (캘린더 X)`: docs 박힘 / 코드 캘린더는 자체 (외부 X) — 일치
- `28행 명단`: docs 박힘 / 시드 21명

---

## 부록 A. 핵심 파일 인덱스

### 데이터 모델
- 작업 시드: `src/shared/tasks.js` (INITIAL_TASKS 12건)
- 작업 형식: `src/data/tasks.js:97` (makeEmptyTask 신규 형식)
- 유솔 N 시드: `src/data/seedTasks.js` (5건)
- 기사 시드: `src/data/engineers.js` (21명)
- 원청 시드: `src/data/principals.js` (6개)
- 사용자 시드: `src/data/users.js` (5역할 + 7건) + `src/shared/users.js` (4역할 + 5건)
- 단가 시드: `src/data/standardRates.js`
- 지역 시드: `src/data/regions.js` (33 지역 + 6 그룹)

### 권한 / 메뉴
- 역할 권한: `src/data/users.js` PERMISSIONS
- 필드 권한 (보기/편집): `src/data/permissions.js`
- 메뉴 권한 (유솔 N): `src/data/menuStructure.js`

### 정산 로직
- 매일 마감: `src/data/dailyClose.js`
- 출장비: `src/data/visitFee.js`
- 일반 수수료: `src/utils/commissionCalc.js`
- 유솔 N 수수료: `src/utils/usolNCommission.js`
- 마진 분석: `src/utils/marginCalculator.js`
- 대시보드 통계: `src/utils/dashboardStats.js`
- 매일 22시 알림: `src/utils/dailyAlertScheduler.js`

### 자동화
- 유솔 N CSV 자동 매칭: `src/utils/usolNAutoMatch.js`
- 유솔 N 추가선택 분류: `src/data/usolNClassify.js`
- 추천 기사 점수: `src/utils/engineerRecommendation.js`

### 메인 페이지
- 진입: `src/App.jsx` (62줄)
- 운영자: `src/pages/AdminApp.jsx` (5942줄)
- 해피콜: `src/pages/HappycallApp.jsx` (2193줄)
- 기사: `src/pages/EngineerApp.jsx` (3237줄)
- 원청: `src/pages/PrincipalApp.jsx` (1416줄)
- 로그인: `src/components/LoginScreen.jsx` (154줄)

### 디자인 시스템
- CSS 변수: `src/styles/themes.js`
- 전역: `src/index.css`
- 칩: `src/components/Chip.jsx` (V12-3 신규)
- 작업 종류 배지: `src/components/ServiceTypeBadge.jsx`
- 운영자 작업상세: `src/components/AdminTaskDetailScreen.jsx` (V12-3 신규)

### SQL
- 스키마 v5: `db/migrations/001_init.sql` (23 테이블)
- DROP: `db/migrations/002_drop_all.sql`

### 외부 통신
- Apps Script: `src/api.js` (URL 1개)
- Supabase: `src/lib/supabase.js` (env 의존)

---

## 부록 B. 빠른 검증 쿼리

```bash
# V-시리즈 적용 흔적
grep -roE "V1[12]-[0-9_]+" src --include="*.jsx" --include="*.js" | grep -v backup | sort -u

# localStorage 키
grep -rn "STORAGE_KEY\s*=" src --include="*.js" | grep -v backup

# screen 분기 (AdminApp)
grep -n "if (screen ===" src/pages/AdminApp.jsx

# 다른 핑크 (V12-3 0건이어야)
grep -rn "#E91E80\|#D81B60\|#C2185B\|#EC407A\|#FF0080" src --include="*.jsx" --include="*.js" --include="*.css" | grep -v backup

# 옅은 핑크 배경 (라이브 0건이어야 — usol_n / SettingsScreen 그라데이션 예외)
grep -rn "background.*rgba(255,27,141,0\." src/components --include="*.jsx" | grep -v backup

# 단색 핫핑크 사용
grep -rn 'background: "#FF1B8D"' src --include="*.jsx" | grep -v backup | wc -l

# UI 텍스트에 "박힘/박힐" (0건이어야 — 자연스러운 한국어만)
grep -rn "박힘\|박힐\|박혀\|박혔" src --include="*.jsx" --include="*.js" --include="*.css" | grep -v backup
```

---

**작성 완료**. 이 문서는 `src/` 코드베이스 + `db/migrations/` SQL 직접 분석 기반. docs/ 17개 .md는 보조 참조용으로만 인용 (불일치 부분 명시). 디자이너/개발자가 다른 대화에 업로드 시 시스템 전체를 파악 가능한 단일 자료.
